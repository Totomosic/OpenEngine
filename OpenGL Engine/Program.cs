using System;
using System.Collections.Generic;

using Pencil.Gaming.Graphics;
using Pencil.Gaming;

/* OpenEngine - Jordan Morrison
 * Basic Setup:
    By default engine uses the default shader program, disable this with Engine.UseDefaultShader = false;
    This will disable .Render calls for basic objects in the engine
    Use ShaderProgram.Default to access the base shader program

    All positions are specified about the center of specified object
    Framebuffer can be accessed by calling Window.Framebuffer

    OpenStates class controls OpenGL State, use this API to alter OpenGL state

    Paths specified to Paths.* require a \ at the end (Windows)

    Texture Cube Maps: right, left, top, bottom, back, front
 * 
 * 
 * */

namespace OpenEngine
{
    class Program
    {

    }
}
